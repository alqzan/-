name: CI

on:
  push:
    branches: ['**']
  pull_request:

jobs:
  check:
    name: فحص الجودة
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          # jsdom يعتمد على undici التي تتطلّب واجهات غير متوفّرة في Node 20
          node-version: 22
          cache: npm

      - run: npm ci

      - name: التحقّق من الأنواع
        run: npm run typecheck

      - name: ESLint
        run: npx eslint .

      - name: الاختبارات
        run: npm test

      - name: البناء
        run: npx vite build
